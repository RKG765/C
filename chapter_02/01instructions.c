#include<stdio.h>

int main(){
    // int a=4,b,c; //Type decleration instructions
    // b = c =a;
    // printf("The value of a is:%d\n",a);
    // printf("The value of a is:%d\n",b);
    // printf("The value of a is:%d\n",c);
     float a=1.2,b;
     b = a+9.8;
     printf("The value of b is%f\n",b);


    
    return 0;
}